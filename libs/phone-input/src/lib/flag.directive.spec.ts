import { FlagDirective } from './flag.directive';

describe('FlagDirective', () => {
    it('should create an instance', () => {
        const directive = new FlagDirective();
        expect(directive).toBeTruthy();
    });
});
